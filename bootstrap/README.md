# Temporary FairScreen bootstrap

Upload `fairscreen-source.tar.gz` into this folder. GitHub Actions will unpack the full application source, commit it to `main`, and remove this temporary folder automatically.
