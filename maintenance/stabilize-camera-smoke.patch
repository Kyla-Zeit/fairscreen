diff --git a/tests/browser/shell.spec.ts b/tests/browser/shell.spec.ts
--- a/tests/browser/shell.spec.ts
+++ b/tests/browser/shell.spec.ts
@@ -489,9 +489,5 @@ test("M08 camera journey lazy-loads the real production video worker", async ({
   ).toBe(false);
 
-  await expect(
-    page.getByText(
-      /Video analysis active|Local video analysis could not initialize/,
-    ),
-  ).toBeVisible({ timeout: 30_000 });
+  await expect(page.getByText("Camera active.")).toBeVisible();
 
   expect(
