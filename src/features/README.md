# Feature Boundary

M02 supplies public education content and M03 supplies settings defaults and the
deterministic demo-data boundary. Remaining folders mark ordered M04+ feature
boundaries; pages continue to depend on typed ports instead of browser APIs.
